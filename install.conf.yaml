- defaults:
    link:
      relink: true
      force: true

- clean: ['~']

- link:
    ~/.config: config
    ~/.gitconfig: gitconfig
    ~/.oh-my-zsh: oh-my-zsh
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim
    ~/.vimrc: vimrc
