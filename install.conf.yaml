- defaults:
    link:
      relink: true
      force: true
      create: true
      ignore-missing: true

- clean: ['~']

- link:
    ~/.config: config
    ~/.oh-my-zsh: oh-my-zsh
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.zshenv: zshenv
